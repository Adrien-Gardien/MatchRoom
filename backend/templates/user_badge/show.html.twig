{% extends 'base.html.twig' %}

{% block title %}UserBadge{% endblock %}

{% block body %}
    <h1>UserBadge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_badge.id }}</td>
            </tr>
            <tr>
                <th>AwardedDate</th>
                <td>{{ user_badge.awardedDate ? user_badge.awardedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_badge_index') }}">back to list</a>

    <a href="{{ path('app_user_badge_edit', {'id': user_badge.id}) }}">edit</a>

    {{ include('user_badge/_delete_form.html.twig') }}
{% endblock %}
