{% extends 'base.html.twig' %}

{% block title %}Search{% endblock %}

{% block body %}
    <h1>Search</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ search.id }}</td>
            </tr>
            <tr>
                <th>Criteria</th>
                <td>{{ search.criteria }}</td>
            </tr>
            <tr>
                <th>SearchDate</th>
                <td>{{ search.searchDate ? search.searchDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_search_index') }}">back to list</a>

    <a href="{{ path('app_search_edit', {'id': search.id}) }}">edit</a>

    {{ include('search/_delete_form.html.twig') }}
{% endblock %}
